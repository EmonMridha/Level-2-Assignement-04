enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
}

enum PaymentProvider {
    STRIPE
    SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}
